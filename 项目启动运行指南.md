# 项目完整启动运行指南

本文档提供从零开始运行整个全栈项目的完整步骤，包括数据库初始化、认证模块配置、后端启动和前端测试。

## 前置准备

### 1. 环境要求
- **Node.js**: >= 18.x
- **pnpm**: >= 8.x
- **PostgreSQL**: >= 15.x（本地或云端数据库）

### 2. 安装 PostgreSQL（如果还没有）

#### macOS (使用 Homebrew)
```bash
brew install postgresql@15
brew services start postgresql@15
```

#### Docker 方式（推荐）
```bash
# 快速启动 PostgreSQL 容器
docker run --name fullstack-postgres \
  -e POSTGRES_PASSWORD=your_password \
  -e POSTGRES_USER=your_user \
  -e POSTGRES_DB=fullstack_db \
  -p 5432:5432 \
  -d postgres:15

# 检查是否运行
docker ps | grep fullstack-postgres
```

#### 云数据库
也可以使用 Supabase、Neon、Railway 等提供的免费 PostgreSQL 数据库。

---

## 完整启动流程

### 步骤 1: 克隆并安装依赖

```bash
# 如果是新项目，先克隆
git clone <your-repo-url>
cd fullstack-boilerplate

# 安装所有依赖（monorepo 会自动安装所有 workspace 的依赖）
pnpm install
```

### 步骤 2: 配置环境变量

#### 2.1 创建根目录环境变量文件
```bash
# 在项目根目录创建 .env 文件
touch .env
```

#### 2.2 配置数据库环境变量（用于数据库迁移）
编辑 `.env` 文件，添加以下内容：

```bash
# ========================================
# 数据库配置（用于 Drizzle 迁移）
# ========================================
# 格式：postgresql://用户名:密码@主机:端口/数据库名
DB_POSTGRES_URL=postgresql://your_user:your_password@localhost:5432/fullstack_db

# 如果使用 Supabase（会有连接池端口 6543）
# DB_POSTGRES_URL=postgresql://postgres:[YOUR-PASSWORD]@db.xxxxx.supabase.co:6543/postgres

# 如果使用 Docker 本地数据库
# DB_POSTGRES_URL=postgresql://your_user:your_password@localhost:5432/fullstack_db
```

#### 2.3 创建后端服务器环境变量
```bash
# 在 apps/server 目录创建 .env 文件
touch apps/server/.env
```

编辑 `apps/server/.env`，添加以下内容：

```bash
# ========================================
# 服务器配置
# ========================================
SERVER_PORT=3001
SERVER_HOST=localhost

# 认证密钥（至少 32 个字符，用于加密 session）
SERVER_AUTH_SECRET=your-super-secret-key-change-this-in-production-minimum-32-chars

# 数据库连接（与根目录相同，用于运行时连接）
SERVER_POSTGRES_URL=postgresql://your_user:your_password@localhost:5432/fullstack_db

# ========================================
# 公开 URL 配置
# ========================================
# 后端 API 地址（用于生成 OpenAPI 文档）
PUBLIC_SERVER_URL=http://localhost:3001
PUBLIC_SERVER_API_PATH=/api

# 前端地址（用于 CORS 配置）
PUBLIC_WEB_URL=http://localhost:3000
```

#### 2.4 创建前端环境变量
```bash
# 在 apps/web 目录创建 .env 文件
touch apps/web/.env
```

编辑 `apps/web/.env`，添加以下内容：

```bash
# ========================================
# 前端 API 配置
# ========================================
PUBLIC_SERVER_URL=http://localhost:3001
PUBLIC_SERVER_API_PATH=api
```

### 步骤 3: 构建共享 Packages

在启动服务前，需要先构建 monorepo 中的共享包：

```bash
# 在项目根目录执行
pnpm build
```

这会构建以下包：
- `@repo/db` - 数据库层
- `@repo/api` - API 合约和客户端
- `@repo/auth` - 认证模块

### 步骤 4: 初始化数据库

#### 4.1 生成 Better Auth 认证表结构

Better Auth 需要生成自己的数据库表（user, session, account 等）：

```bash
# 在项目根目录执行
cd packages/auth

# 生成认证相关的数据库表结构
pnpm better-auth generate

# 或者如果 package.json 中配置了脚本
# pnpm auth:generate
```

这会在 `packages/db/src/schemas/auth.ts` 中生成 Better Auth 所需的表结构。

#### 4.2 推送数据库 Schema

现在将所有 schema（包括 posts、user、session 等）推送到数据库：

```bash
# 回到项目根目录
cd ../..

# 推送 schema 到数据库（会创建所有表）
cd packages/db
pnpm push

# 或者使用 drizzle-kit 生成迁移文件（推荐用于生产环境）
# pnpm drizzle-kit generate
# pnpm drizzle-kit migrate
```

**注意**：
- `pnpm push` 会直接修改数据库结构，适合开发环境快速迭代
- 生产环境建议使用 `generate` + `migrate` 方式，可以追踪迁移历史

#### 4.3 验证数据库表

使用 Drizzle Studio 查看数据库：

```bash
# 在 packages/db 目录
pnpm studio

# 会启动一个 Web UI：https://local.drizzle.studio
```

你应该能看到以下表：
- `user` - 用户表
- `session` - 会话表
- `account` - 账户表（OAuth 用）
- `verification` - 验证表（邮箱验证用）
- `post` - 文章表

### 步骤 5: 启动后端服务器

```bash
# 在新的终端窗口，进入后端目录
cd apps/server

# 启动开发服务器（会自动监听文件变化）
pnpm dev
```

**预期输出**：
```
Server running at http://localhost:3001
API Documentation at http://localhost:3001/api/doc
```

#### 验证后端是否正常运行

在浏览器或使用 curl 访问：

```bash
# 查看 API 文档
open http://localhost:3001/api/doc

# 或使用 curl 测试健康检查接口（如果有）
curl http://localhost:3001/api/health
```

### 步骤 6: 启动前端应用

```bash
# 在新的终端窗口，进入前端目录
cd apps/web

# 启动前端开发服务器
pnpm dev
```

**预期输出**：
```
VITE ready in 500ms
➜  Local:   http://localhost:3000/
➜  Network: use --host to expose
```

### 步骤 7: 测试完整流程

#### 7.1 注册新用户

1. 打开浏览器访问 `http://localhost:3000`
2. 找到注册/登录页面
3. 使用邮箱密码注册新账户（Better Auth 配置了 `autoSignIn: true`，会自动登录）

#### 7.2 测试 API 接口

假设你要测试 posts 接口：

```typescript
// 在前端代码中使用（例如 apps/web/src/routes/post/index.tsx）
import { apiClient } from '@/integrations/tanstack-query/apiClient';

export default function PostsPage() {
  // 获取所有文章 - 自动类型安全
  const { data: posts, isLoading } = apiClient.posts.all.useQuery();

  // 创建文章
  const createMutation = apiClient.posts.create.useMutation({
    onSuccess: () => {
      // 刷新列表
      queryClient.invalidateQueries(['posts']);
    },
  });

  const handleCreate = () => {
    createMutation.mutate({
      title: 'My First Post',
      content: 'This is a test post',
    });
  };

  if (isLoading) return <div>Loading...</div>;

  return (
    <div>
      <button onClick={handleCreate}>Create Post</button>
      <ul>
        {posts?.map(post => (
          <li key={post.id}>{post.title}</li>
        ))}
      </ul>
    </div>
  );
}
```

#### 7.3 使用浏览器测试

1. 访问 `http://localhost:3000`
2. 登录账户
3. 测试创建、查看、删除文章等功能

#### 7.4 使用 API 文档测试

访问 `http://localhost:3001/api/doc`，可以直接在 Scalar UI 中测试 API：

1. 点击 "Try it" 按钮
2. 输入请求参数
3. 查看响应结果
4. 验证类型是否正确

---

## 常见问题和解决方案

### 问题 1: 数据库连接失败

**错误信息**：
```
Error: connect ECONNREFUSED 127.0.0.1:5432
```

**解决方案**：
1. 检查 PostgreSQL 是否正在运行：
   ```bash
   # macOS/Linux
   pg_isready
   
   # Docker
   docker ps | grep postgres
   ```

2. 验证数据库连接字符串是否正确
3. 确保数据库用户有足够的权限

### 问题 2: Better Auth 表未生成

**错误信息**：
```
Error: relation "user" does not exist
```

**解决方案**：
```bash
# 重新生成 Better Auth schema
cd packages/auth
pnpm better-auth generate

# 推送到数据库
cd ../db
pnpm push
```

### 问题 3: 前端无法连接后端

**错误信息**：
```
Network Error / CORS Error
```

**解决方案**：
1. 确保后端正在运行：`curl http://localhost:3001/api/health`
2. 检查前端的 `.env` 文件中 `PUBLIC_SERVER_URL` 是否正确
3. 检查后端的 `PUBLIC_WEB_URL` 配置（用于 CORS）
4. 清除浏览器缓存或使用无痕模式测试

### 问题 4: 类型错误

**错误信息**：
```
Type 'X' is not assignable to type 'Y'
```

**解决方案**：
```bash
# 重新构建所有 packages
pnpm build

# 重启 TypeScript 服务器（VSCode）
# Command Palette -> TypeScript: Restart TS Server
```

### 问题 5: 环境变量未加载

**解决方案**：
1. 确保 `.env` 文件在正确的位置
2. 重启开发服务器
3. 检查变量名拼写是否正确
4. 对于 Vite，确保变量以 `PUBLIC_` 开头才能在客户端使用

---

## 开发工作流总结

### 日常开发启动流程

```bash
# Terminal 1: 启动后端
cd apps/server && pnpm dev

# Terminal 2: 启动前端
cd apps/web && pnpm dev

# Terminal 3（可选）: 查看数据库
cd packages/db && pnpm studio
```

### 修改 Schema 后的流程

```bash
# 1. 修改 packages/db/src/schemas/*.schema.ts

# 2. 如果修改了共享包，重新构建
pnpm build

# 3. 推送到数据库
cd packages/db
pnpm push

# 4. 重启后端服务器（如果使用 tsx watch 会自动重启）
```

### 新增 API 接口流程

```bash
# 1. 定义 DB Schema
# packages/db/src/schemas/your-table.schema.ts

# 2. 定义 API Contract
# packages/api/src/contracts/your-route.ts

# 3. 实现 Server Router
# packages/api/src/server/router/your-route.ts

# 4. 注册路由
# packages/api/src/contracts/index.ts
# packages/api/src/server/router/index.ts

# 5. 重新构建
pnpm build

# 6. 前端直接使用（自动获得类型）
# apiClient.yourRoute.yourMethod.useQuery()
```

---

## 生产环境部署检查清单

在部署到生产环境前，确保：

- [ ] 更改 `SERVER_AUTH_SECRET` 为强密码（至少 32 位随机字符）
- [ ] 使用生产数据库（不要使用本地数据库）
- [ ] 配置正确的 `PUBLIC_SERVER_URL` 和 `PUBLIC_WEB_URL`
- [ ] 运行 `pnpm build` 构建所有包
- [ ] 数据库使用 `drizzle-kit generate` 和 `migrate` 而非 `push`
- [ ] 配置 HTTPS 证书
- [ ] 设置 CORS 白名单
- [ ] 启用速率限制和安全中间件
- [ ] 配置日志和监控
- [ ] 设置数据库备份策略

---

## 快速参考

### 常用命令

```bash
# 安装依赖
pnpm install

# 构建所有 packages
pnpm build

# 推送数据库 schema
cd packages/db && pnpm push

# 查看数据库
cd packages/db && pnpm studio

# 启动后端
cd apps/server && pnpm dev

# 启动前端
cd apps/web && pnpm dev

# 格式化代码
pnpm format

# 检查代码质量
pnpm lint
```

### 端口占用

- **前端**: `http://localhost:3000`
- **后端**: `http://localhost:3001`
- **API 文档**: `http://localhost:3001/api/doc`
- **Drizzle Studio**: `https://local.drizzle.studio`

### 目录结构快速参考

```
fullstack-boilerplate/
├── .env                          # 数据库配置
├── apps/
│   ├── server/
│   │   ├── .env                 # 后端环境变量
│   │   └── src/server.ts        # 后端入口
│   └── web/
│       ├── .env                 # 前端环境变量
│       └── src/router.tsx       # 前端路由
└── packages/
    ├── db/                      # 数据库 Schema
    ├── api/                     # API 合约和路由
    └── auth/                    # 认证配置
```

---

## 总结

通过以上步骤，你的完整全栈项目应该已经成功运行。主要流程是：

1. **准备环境** → 安装 Node.js、pnpm、PostgreSQL
2. **配置环境变量** → 在根目录、后端、前端分别配置
3. **构建 Packages** → `pnpm build`
4. **初始化数据库** → Better Auth 生成表 → 推送 Schema
5. **启动服务** → 后端 `pnpm dev` → 前端 `pnpm dev`
6. **测试功能** → 注册用户 → 测试 API

现在你可以开始开发新功能了！🎉
